// Package assistant is the root of the assistant module. It holds no code;
// it exists so tooling that resolves the repository root as a Go package
// (e.g. `swag init`) can determine the package name.
package assistant
